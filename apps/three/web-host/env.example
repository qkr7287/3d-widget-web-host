## web-host 환경변수 예시 (커밋용)
#
# 운영/개발 환경에서 원격 위젯 모듈 URL을 바꾸고 싶다면
# 이 파일 내용을 참고해서 `apps/web-host/.env` (또는 `.env.local`)을 만들어 사용하세요.
#
# 예)
#   VITE_WIDGET_EMBED_URL=https://cdn.example.com/widget/embed.js
#
# 1) 가장 단순: embed URL을 직접 고정(우선순위 가장 높음)
VITE_WIDGET_EMBED_URL=

# 2) 또는 host/port/protocol로 조합해서 사용(DEV에서는 /src/embed.ts, PROD에서는 /embed.js)
VITE_WIDGET_PROTOCOL=http
VITE_WIDGET_HOST=192.168.0.47
VITE_WIDGET_PORT=5174

