## web-host 서버(Vite) 설정 예시 (커밋용)
#
# `apps/web-host/.env` 또는 `.env.local`에 복사해서 사용하세요.
#
# - VITE_HOST=true  -> 0.0.0.0 바인딩 (LAN 접속 가능)
# - VITE_HOST=127.0.0.1 -> 로컬만
# - VITE_PORT=5173 -> dev/preview 포트

VITE_HOST=true
VITE_PORT=5173

