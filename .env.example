# ==========================================
# 3d-widget-web-host 환경 변수 예시
# ==========================================
# .env.full / .env.webhost 복사 후 값 수정해서 사용하세요.
#
# full 컨테이너: 3개 앱 모두 실행 (dev:compare)
# webhost-only 컨테이너: web-host만 실행, 위젯은 full 서비스에서 로드
# ==========================================

# Vite dev 서버 바인딩 (true=0.0.0.0, "127.0.0.1"=로컬만)
DEV_SERVER_HOST=true

# 각 앱 포트
PORT_WEB_HOST=5173
PORT_3D_WIDGET=5174
PORT_COMBINED_APP=6100

# web-host가 3d-widget을 가져올 주소
# - 빈 값: 현재 접속 host 사용
# - webhost-only에서 full 서비스 참조 시: WIDGET_HOST=full
WIDGET_PROTOCOL=http
WIDGET_HOST=
WIDGET_EMBED_URL_OVERRIDE=
